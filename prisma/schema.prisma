datasource db {
  provider = "postgresql"
  url      = env("DIRECT_DB_CONNECTION")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}